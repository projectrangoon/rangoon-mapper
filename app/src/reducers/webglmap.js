const initialState = {
  center: [ 96.1518985, 16.7943528 ],
  zoom: [11],
  bearing: 1,
  pitch: 30,
};

const webglmap = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default webglmap;
